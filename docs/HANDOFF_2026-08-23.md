# Ramblers handoff — 2026-08-23

This handoff closes the development thread that produced the `0.17.0` candidate. The accumulated work was preserved first at commit `4eb9ced3`, then reconstructed as a dependency-ordered draft stack:

1. `bl/action-runtime-foundation` — runtime source and native-action architecture.
2. `bl/action-protocol-verification` — deterministic probes, protocol checks, and runtime-invariant scripts.
3. `bl/action-handoff-and-artifacts` — documentation, reviewed build artifacts, and this handoff.

The branches are stacked because the verification layer depends on the runtime layer, and the checked-in artifact and documentation describe both. Do not describe the stack as runtime-verified.

## Decisive current blocker

Ordinary nearby button use is still broken. This is not limited to carrying or the crane puzzle.

Latest reproduction:

- Level 1 starting area.
- Human and companion were next to the button, within ordinary arm's reach.
- Human looked directly at and pointed at the button.
- Human had personally pressed the same button several times.
- Request: press the button.

Exact latest-run evidence from `C:\Program Files (x86)\Steam\steamapps\common\Big Walk\BepInEx\LogOutput.log`:

```text
321 [INTERACT] WORLD_REFERENCE_CAPTURED source=stock_castable, instanceId=1216508
322 [INTERACT] STRUCTURAL_AFFORDANCE_SCANNED ... source=HumanReference, outcomes=1, constructible=1
328 [AGENT] TURN_INTERACTION_REFERENCES_CAPTURED ... humanReferenceId=switch:local:1251484, humanKind=world_switch
342 [INTERACT] STRUCTURAL_AFFORDANCE_SCANNED ... source=ContextEntity, outcomes=1, constructible=1
343 [ENTITY] TARGET_RESOLVED ... target=interaction:local:1216508, referenceId=switch:local:1251484, kind=world_switch
344 [AGENT] CALL name=interact_with_object ... result={"ok":false,"status":"game_action_unavailable"}, diagnosticError=interaction_conditions_changed
```

This proves:

- speech-bound exact capture worked;
- the exact `CastableTarget` had one constructible `PeckSwitch` outcome;
- context resolution selected the same exact switch;
- failure occurred synchronously before the interaction job started;
- there was no approach, carry transition, native authority call, or receipt.

It does **not** prove a raycast failure. Earlier carried-switch logging combined `CanStillReachSwitch` and `IsSafeToUseSwitch`, so any statement assigning that older failure to one predicate was an inference, not evidence.

## Most likely code seam to inspect first

Follow the structural-to-dynamic binding in `CompanionAffordanceTarget.TryCaptureWorldOutcome` and `CompanionWorldSwitchAffordanceDriver.TryValidateOutcome`.

The exact switch is structurally available from the human's cast, but the context-selected driver subsequently asks `CastableTarget.GetCastableOutcome` for the companion actor. That actor-dependent rematerialization appears to return no matching outcome, producing `interaction_conditions_changed` before a job exists. Confirm this with predicate-level telemetry before changing admission.

Do not add a Level 1 name, button name, crane branch, nearest-object fallback, or silent retarget. The intended generic lifecycle remains:

```text
utterance-frozen exact CastableTarget
→ one exact supported native primitive
→ actor/movement policy
→ alignment
→ final dynamic safety and reach
→ stock host authority
→ exact authoritative receipt
```

The unresolved design question is which actor-dependent checks belong at structural binding versus final activation for a host-controlled non-local companion. Preserve exact component identity, blockers, prerequisite state, authority, and postcondition confirmation while answering it.

## Evidence status

Source/build proof:

- `bl/action-runtime-foundation` builds independently against Big Walk `1.4.10`.
- `bl/action-protocol-verification` passes all 23 local protocol tests.
- The same-path deterministic `0.17.0` build produced SHA-256 `6226ADFAF73173E92493BBD3F3D1C09B4F18355F1F0C5C3BA5571B63A008F7BD`.
- That DLL was deployed to `BepInEx\plugins\Ramblers\Ramblers.dll` before the latest reproduction.

Runtime proof:

- The latest run loaded the deployed candidate and captured/resolved the exact nearby switch.
- The button action failed before job start with `interaction_conditions_changed`.
- Therefore live button activation is explicitly **not verified**.

## Open repository work

- [#47 — Let Rambler walk to and use named nearby switches](https://github.com/benceruleanlu/Ramblers/issues/47): primary blocker; update with the exact trace above.
- [#43 — Add deterministic action-sequence tests](https://github.com/benceruleanlu/Ramblers/issues/43): the new protocol runner materially advances this, but local probes still do not replace Unity integration/runtime evidence.
- [#48 — Match synthetic speech loudness and falloff](https://github.com/benceruleanlu/Ramblers/issues/48): safe metre-domain attenuation remains, but exact network-player mixer-route parity is intentionally unresolved after the earlier CoreCLR crash.
- [PR #29 — Remove unread fields from SteeringStatus](https://github.com/benceruleanlu/Ramblers/pull/29): unrelated pre-existing cleanup; do not mix it into this stack.

## Recommended next-agent scope

Work only issue #47 first. Add predicate-level logs around dynamic outcome rematerialization, reproduce the nearby Level 1 button once, and use the exact-run trace to correct the structural/dynamic seam. Do not broaden into carry, pathfinding, seats, item homes, or puzzle-specific behavior until a nearby ordinary button reaches native authority and confirms its exact switch state.

The user should only be asked for one visual QA pass after source checks, the full protocol suite, deterministic build, deployment-hash verification, and an independent review are complete. Do not launch or focus the game on the user's behalf.
